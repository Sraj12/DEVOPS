pipeline {
    agent any
    
    stages {
        stage('Download') {
            steps {
                sh 'echo "artifact file download" > File.txt'
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'File.txt', onlyIfSuccessful: true
        }
    }
}
